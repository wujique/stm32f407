修改记录详细记录

20190306
	增加电子纸 IL3820驱动。

20190227
	修改：
	1 增加电纸书驱动91874。
	2 修改LCD硬件接口总线，增加一条sta管脚。
	
	备忘
	1 91874的通信，每个字节都需要操作CS，很特殊，暂时放在本驱动，先不修改LCD BUS。
	2 91874有一根BUSY管脚，这个管脚，应该是属于LCD 硬件接口层的。放在驱动不是很合适。
	3 电子纸刷新很慢，最好能改为：指定显示区域，填充显示缓冲，refresh。三步式。
	  等评估后，将所有LCD的驱动接口改为支持三步式。



/* 碎碎念 */
s32 bus_lcd_write_data(DevLcdBusNode *node, u8 *data, u32 len)
data定义是一个u8的指针
彩屏的LCD BUS，用U16